import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'anw-react-ionic-starter',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
